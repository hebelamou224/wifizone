import { ArraySizePipe } from './array-size.pipe';

describe('ArraySizePipe', () => {
  it('create an instance', () => {
    const pipe = new ArraySizePipe();
    expect(pipe).toBeTruthy();
  });
});
